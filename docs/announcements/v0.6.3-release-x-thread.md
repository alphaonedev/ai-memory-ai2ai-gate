<!--
Copyright 2026 AlphaOne LLC
SPDX-License-Identifier: Apache-2.0

X / Twitter thread draft — ai-memory v0.6.3 release announcement.

Audience: developers + procurement engineers + the broader memory-
infrastructure-curious. Anchor: the v0.6.3 "What's New" page, which is
the procurement-defensible source for every claim in this thread.

  https://alphaonedev.github.io/ai-memory-mcp/whats-new-v063.html

Every quantitative claim in this thread is sourced from that page or from
docs/evidence.html. No invented numbers. Numbers double-checked against
the page on 2026-04-29.

POSTING NOTES (operator):
- Author account: @Alpha_One_AI
- AI NHI cannot post directly — operator-published per the v0.6.0 +
  v0.6.2 + v0.6.2-Opus pattern (memory IDs 2c80b41b, 5aa0809b, f90b0144)
- Cadence: 1 tweet ~10s sequential; numbered (1/9, 2/9, …)
- Sequence: this thread FIRST (release announcement). The architectures
  thread (PR #67) goes SECOND, on a later day, as evergreen reference.

Total: 9 tweets.
-->

# ai-memory v0.6.3 — release announcement thread

**Author:** @Alpha_One_AI
**Anchor URL:** https://alphaonedev.github.io/ai-memory-mcp/whats-new-v063.html
**Total tweets:** 9
**Posting cadence:** 1 tweet ~10s, sequential
**Sequence:** publish FIRST. Architectures thread (PR #67) follows next week.

---

## 1/9 — Hero

```
ai-memory v0.6.3 — the grand-slam release.

Three pillars in one focused 4-week sprint:
• Hierarchical namespace tree
• Temporal-validity knowledge graph
• CI-enforced sub-100ms performance budgets

56.7% → 93.08% code coverage · 1,809 tests · 0 ignored

→ https://alphaonedev.github.io/ai-memory-mcp/whats-new-v063.html

1/9
```

---

## 2/9 — Pillar 1 · Hierarchy

```
PILLAR 1 — Hierarchy.

Namespaces gain /-delimited tree paths. Recall walks ancestors. memory_get_taxonomy returns the full tree with per-node memory counts.

Lossless flat → hierarchy migration. Subtree-scoped recall via prefix match. Ships today.

→ https://alphaonedev.github.io/ai-memory-mcp/hierarchies.html

2/9
```

---

## 3/9 — Pillar 2 · Knowledge Graph (Schema + Entities)

```
PILLAR 2 — Knowledge Graph.

Schema v15: memory_links gains valid_from / valid_until / observed_by / signature columns. entity_aliases side table. Temporal indexes.

The signature column is reserved for v0.7 Ed25519. Code path is wired today; cryptographic attestation lands next.

→ https://alphaonedev.github.io/ai-memory-mcp/knowledge-graph.html

3/9
```

---

## 4/9 — Pillar 2 · KG Query Layer

```
PILLAR 2 — KG Query Layer.

Recursive-CTE traversal with time + scope + observed-by filters.

• memory_kg_query — depth ≤ 5
• memory_kg_timeline — ordered facts
• memory_kg_invalidate — sets valid_until on a link
• Cypher stubs for v0.7 AGE wrapper

Cycle detection via path-LIKE check. Ships today.

4/9
```

---

## 5/9 — Pillar 2 · Duplicate Check

```
PILLAR 2 — Duplicate Check.

memory_check_duplicate — pre-write similarity check using the existing embedding pipeline. Returns nearest-neighbor cosine + above-threshold flag + suggested merge target.

Configurable threshold (default 0.85). Tier-aware: requires semantic+ for embeddings.

5/9
```

---

## 6/9 — Pillar 3 · Performance + CI Budgets

```
PILLAR 3 — Performance.

ai-memory bench: canonical 1k-memory workload, p50 / p95 / p99 reporting per operation, --baseline regression detection.

PERFORMANCE.md is authoritative. bench.yml runs on every PR. Build fails if any p95 exceeds budget by >10%.

→ https://alphaonedev.github.io/ai-memory-mcp/performance.html

6/9
```

---

## 7/9 — The coverage campaign

```
The W3-W12 code-coverage campaign closed at 93.08% line coverage.

• Lines: 56.7% → 93.08% (cargo llvm-cov)
• 1,809 tests passing (1,600 lib + 209 integration)
• 0 failed · 0 ignored
• 9 waves · 26 closers · ~1,200 net new tests
• Apple M4 reference hardware

Frozen claims → https://alphaonedev.github.io/ai-memory-mcp/evidence.html

7/9
```

---

## 8/9 — Security

```
Two SSRF defects discovered + fixed during the v0.6.3 cycle.

Both shipped with rc1. Both #[ignore] tests are now un-ignored and passing. Zero ignored tests across the whole 1,809-test suite.

CI gate: ≥92% line coverage (cargo llvm-cov). v0.6.3 passes at 93.08% with a 1% absorb buffer.

8/9
```

---

## 9/9 — Install + close

```
Install in 60s — ai-memory v0.6.3.

curl -fsSL https://raw.githubusercontent.com/alphaonedev/ai-memory-mcp/main/install.sh | sh

Apache 2.0; trademark-protected (USPTO 99761257). Self-hosted, local-first, zero cloud dependency.

What's new → https://alphaonedev.github.io/ai-memory-mcp/whats-new-v063.html

9/9
```

---

## Per-tweet anchor URLs (operator paste sheet)

| # | Topic | Primary URL |
|---|---|---|
| 1 | Hero / what's new | https://alphaonedev.github.io/ai-memory-mcp/whats-new-v063.html |
| 2 | Pillar 1 — Hierarchy | https://alphaonedev.github.io/ai-memory-mcp/hierarchies.html |
| 3 | Pillar 2 — KG Schema | https://alphaonedev.github.io/ai-memory-mcp/knowledge-graph.html |
| 4 | Pillar 2 — KG Query | (no anchor — body is self-contained) |
| 5 | Pillar 2 — Duplicate check | (no anchor — body is self-contained) |
| 6 | Pillar 3 — Performance | https://alphaonedev.github.io/ai-memory-mcp/performance.html |
| 7 | Coverage campaign | https://alphaonedev.github.io/ai-memory-mcp/evidence.html |
| 8 | Security | (no anchor — body is self-contained) |
| 9 | Install + close | https://alphaonedev.github.io/ai-memory-mcp/whats-new-v063.html |

---

## Data sources (every claim is sourced)

| Claim in thread | Source |
|---|---|
| `56.7% → 93.08% code coverage (line)` | whats-new-v063.html · evidence.html · cargo llvm-cov |
| `1,809 tests passing` | whats-new-v063.html · evidence.html (1,600 lib + 209 integration) |
| `0 failed · 0 ignored` | whats-new-v063.html "By the Numbers" tile |
| `9 waves · 26 closers · ~1,200 net new tests` | whats-new-v063.html "By the Numbers" tile |
| `Streams A-F` (six streams across three pillars) | whats-new-v063.html "Six Engineering Streams" |
| `memory_get_taxonomy / MAX_NAMESPACE_DEPTH` (Pillar 1) | whats-new-v063.html Stream A |
| `valid_from / valid_until / observed_by / signature` (KG schema v15) | whats-new-v063.html Stream B |
| `memory_kg_query / memory_kg_timeline / memory_kg_invalidate` | whats-new-v063.html Stream C |
| `memory_check_duplicate · 0.85 default threshold` | whats-new-v063.html Stream D |
| `bench: 1k-memory canonical workload, p50/p95/p99` | whats-new-v063.html Stream E |
| `bench.yml CI gate, >10% p95 budget tolerance` | whats-new-v063.html Stream F |
| `2 SSRF defects fixed` | whats-new-v063.html security card |
| `≥92% line coverage gate` | whats-new-v063.html + .github/workflows/ci.yml |
| `Apple M4 reference hardware` | whats-new-v063.html "By the Numbers" caption |
| `USPTO Serial No. 99761257` | docs/index.html footer + evidence.html |
| `Apache 2.0 license` | LICENSE file |

---

## Character count audit (X limit: 280; URL = 23)

| Tweet | Body chars | URLs | Estimated total | Margin to 280 |
|---|---|---|---|---|
| 1/9 | ~228 | 1 | ~251 | 29 |
| 2/9 | ~226 | 1 | ~249 | 31 |
| 3/9 | ~262 | 1 | ~285 | -5 ⚠ |
| 4/9 | ~232 | 0 | ~232 | 48 |
| 5/9 | ~261 | 0 | ~261 | 19 |
| 6/9 | ~241 | 1 | ~264 | 16 |
| 7/9 | ~249 | 1 | ~272 | 8 |
| 8/9 | ~253 | 0 | ~253 | 27 |
| 9/9 | ~234 | 1 | ~257 | 23 |

⚠ Tweet 3/9 is borderline. Operator-fix option: drop the second sentence's
"The signature column is reserved for v0.7 Ed25519. Code path is wired
today; cryptographic attestation lands next." → replace with
"signature column reserved for v0.7 Ed25519." → fits.

Recheck character counts using the X composer's counter before posting —
the counts above are estimates; emojis, CJK characters, and certain
Unicode punctuation have variable weight.

---

## Posting order (relative to architectures thread)

This release thread (v0.6.3-release-x-thread.md) goes **first** — release
announcement is time-sensitive and the v0.6.3 cut on 2026-04-26 is already
3 days stale on the wire.

The architectures thread (v0.6.3-architectures-x-thread.md, PR #67) goes
**second**, on a later day, as evergreen reference content. The architectures
thread builds on the vocabulary established here ("hierarchy", "KG",
"performance budgets", "coverage campaign"); shipping it after gives the
audience two compounding messages, not two competing ones.

---

## What this thread DOES NOT claim

For audit defensibility — this thread deliberately omits:

- **Ed25519 attestation as a shipping feature** — it's not. Schema column
  is reserved; binary doesn't sign or verify in v0.6.3. v0.7 work.
- **Forensic export bundles / hash-chained audit logs / sidechain
  transcripts** — all v0.7 forensic substrate. Out of scope for the
  release thread; will land in a separate v0.7-preview thread when v0.7
  ships.
- **AgenticMem operational counterparty** — separate brand, separate
  message; the architectures thread is the right surface for the
  "substrate, not product" framing if needed.
- **SOC 2 / ISO 27001 / FedRAMP / HIPAA** — not held by ai-memory.
  Evidence page explicitly states this; the thread doesn't imply
  otherwise.

---

## AI involvement

- **Drafted by:** Claude Opus 4.7 (1M context) under operator direction
  ("make sure the X releases thread has data" + URL pointer to
   whats-new-v063.html as the canonical source)
- **Auth class:** Standard (announcement draft only — operator-published)
- **Workflow:** read template (v0.6.2-x-thread.md) → fetch real data from
  whats-new-v063.html → cross-check against evidence.html canonical →
  draft 9-tweet thread with explicit per-tweet data citations → flag one
  borderline tweet with operator-fix option → audit what's intentionally
  omitted to keep the thread procurement-defensible

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
